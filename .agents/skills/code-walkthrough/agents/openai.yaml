interface:
  display_name: "Code Walkthrough Comparison"
  short_description: "Compare upstream Diffmap with the custom review"
  default_prompt: "Use $code-walkthrough to review these changes and return both the upstream and custom local URLs."
