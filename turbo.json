{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "!.next/dev/**"]
    },
    "lint": {
      "dependsOn": ["^lint"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/.oxlintrc.json",
        "$TURBO_ROOT$/tools/oxlint/anti-slop/**",
        "$TURBO_ROOT$/tools/oxlint/no-unused-exports/**",
        "$TURBO_ROOT$/tools/vitest/**"
      ]
    },
    "lint:fix": {
      "dependsOn": ["^lint:fix"],
      "cache": false,
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/.oxlintrc.json",
        "$TURBO_ROOT$/tools/oxlint/anti-slop/**",
        "$TURBO_ROOT$/tools/oxlint/no-unused-exports/**",
        "$TURBO_ROOT$/tools/vitest/**"
      ]
    },
    "format": {
      "dependsOn": ["^format"],
      "cache": false,
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/.oxfmtrc.json",
        "$TURBO_ROOT$/tools/vitest/**"
      ]
    },
    "format:check": {
      "dependsOn": ["^format:check"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/.oxfmtrc.json",
        "$TURBO_ROOT$/tools/vitest/**"
      ]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/tools/vitest/**"]
    },
    "test:unit": {
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/tools/vitest/**"]
    },
    "test:e2e": {
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/tools/vitest/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
