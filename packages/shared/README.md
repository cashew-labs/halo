# Shared runtime utilities

This package owns reusable primitives such as `Stream` and `SerialQueue`.
Workspace protocol types, client construction, and session reducers live in
[`@get-halo/client`](../client/README.md).

The control-plane contract, server discovery format, and test-description helpers
remain here until their ownership is addressed in later migration phases.
